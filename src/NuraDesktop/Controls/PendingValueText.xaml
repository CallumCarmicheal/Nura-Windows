<UserControl x:Class="NuraDesktop.Controls.PendingValueText"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="White"
             IsHitTestVisible="False">
    <Grid ClipToBounds="True">
        <TextBlock x:Name="BaseText"
                   Text="{Binding Text, ElementName=Root}"
                   FontSize="{Binding FontSize, ElementName=Root}"
                   FontWeight="{Binding FontWeight, ElementName=Root}"
                   TextWrapping="{Binding TextWrapping, ElementName=Root}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{Binding Foreground, ElementName=Root}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPending, ElementName=Root}" Value="True">
                            <Setter Property="Foreground" Value="#8CFFFFFF" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <TextBlock x:Name="ShimmerText"
                   Text="{Binding Text, ElementName=Root}"
                   FontSize="{Binding FontSize, ElementName=Root}"
                   FontWeight="{Binding FontWeight, ElementName=Root}"
                   TextWrapping="{Binding TextWrapping, ElementName=Root}"
                   Foreground="White"
                   Opacity="0">
            <TextBlock.OpacityMask>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <LinearGradientBrush.RelativeTransform>
                        <TranslateTransform x:Name="ShimmerTransform" X="-1" />
                    </LinearGradientBrush.RelativeTransform>
                    <GradientStop Color="#00FFFFFF" Offset="0" />
                    <GradientStop Color="#FFFFFFFF" Offset="0.45" />
                    <GradientStop Color="#00FFFFFF" Offset="1" />
                </LinearGradientBrush>
            </TextBlock.OpacityMask>
        </TextBlock>
    </Grid>
</UserControl>
