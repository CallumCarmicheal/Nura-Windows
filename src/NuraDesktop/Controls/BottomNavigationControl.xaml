<UserControl x:Class="NuraPopupWpf.Controls.BottomNavigationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UniformGrid Columns="2">
        <Button Command="{Binding ShowDevicePageCommand}" Margin="0,0,8,0">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource BottomNavButtonStyle}">
                    <Setter Property="Background" Value="#00000000" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDevicePage}" Value="True">
                            <Setter Property="Background" Value="#26FF57B9" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE7F5;"
                           FontSize="14"
                           Margin="0,0,8,0"
                           VerticalAlignment="Center" />
                <TextBlock Text="Device"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
            </StackPanel>
        </Button>
        <Button Command="{Binding ShowSettingsPageCommand}" Margin="8,0,0,0">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource BottomNavButtonStyle}">
                    <Setter Property="Background" Value="#00000000" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSettingsPage}" Value="True">
                            <Setter Property="Background" Value="#26FF57B9" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE713;"
                           FontSize="14"
                           Margin="0,0,8,0"
                           VerticalAlignment="Center" />
                <TextBlock Text="Settings"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
            </StackPanel>
        </Button>
    </UniformGrid>
</UserControl>
