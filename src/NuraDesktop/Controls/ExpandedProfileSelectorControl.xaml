<UserControl x:Class="NuraPopupWpf.Controls.ExpandedProfileSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:Width="420"
             d:Height="54">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="SlashHighlightBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFD29FFF" Offset="0" />
            <GradientStop Color="#FFFF63E3" Offset="0.52" />
            <GradientStop Color="#FFFF71B4" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ProfileSelectorButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource MutedSurfaceBrush}"
            BorderBrush="#22FFFFFF"
            BorderThickness="1"
            CornerRadius="20"
            Padding="5"
            SnapsToDevicePixels="True">
        <Grid x:Name="LayoutRoot"
              Height="38"
              ClipToBounds="False">
            <Canvas x:Name="SelectionCanvas"
                    IsHitTestVisible="False"
                    ClipToBounds="False">
                <Border x:Name="ActivePill"
                        Width="0"
                        Height="34"
                        CornerRadius="0"
                        Background="{StaticResource AccentGradientBrush}"
                        Opacity="0.96"
                        Visibility="Collapsed"
                        SnapsToDevicePixels="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="18"
                                          ShadowDepth="0"
                                          Color="#3DD946EF"
                                          Opacity="0.6" />
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="ActivePillTransform" X="0" />
                    </Border.RenderTransform>
                </Border>
            </Canvas>

            <Grid x:Name="SegmentGrid" ClipToBounds="False" />
        </Grid>
    </Border>
</UserControl>
