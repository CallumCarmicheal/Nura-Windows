<UserControl x:Class="NuraPopupWpf.Controls.OtpCodeInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:Width="320"
             d:Height="60">
    <UserControl.Resources>
        <Style x:Key="OtpDigitTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="54" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="#14FFFFFF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#24FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="CaretBrush" Value="#FFFF73B9" />
            <Setter Property="MaxLength" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#88FF73B9" />
                                <Setter TargetName="Chrome" Property="Background" Value="#1CFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#18FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="DigitBox0" Grid.Column="0" Style="{StaticResource OtpDigitTextBoxStyle}" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
        <TextBox x:Name="DigitBox1" Grid.Column="1" Style="{StaticResource OtpDigitTextBoxStyle}" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
        <TextBox x:Name="DigitBox2" Grid.Column="2" Style="{StaticResource OtpDigitTextBoxStyle}" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
        <TextBox x:Name="DigitBox3" Grid.Column="3" Style="{StaticResource OtpDigitTextBoxStyle}" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
        <TextBox x:Name="DigitBox4" Grid.Column="4" Style="{StaticResource OtpDigitTextBoxStyle}" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
        <TextBox x:Name="DigitBox5" Grid.Column="5" Style="{StaticResource OtpDigitTextBoxStyle}" Margin="0" PreviewTextInput="DigitBox_OnPreviewTextInput" PreviewKeyDown="DigitBox_OnPreviewKeyDown" TextChanged="DigitBox_OnTextChanged" DataObject.Pasting="DigitBox_OnPasting" />
    </Grid>
</UserControl>
