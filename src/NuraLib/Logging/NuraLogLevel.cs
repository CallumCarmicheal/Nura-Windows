namespace NuraLib.Logging;

/// <summary>
/// Describes the importance and intended verbosity of a library log message.
/// </summary>
public enum NuraLogLevel {
    Trace = 0,
    Debug = 1,
    Information = 2,
    Warning = 3,
    Error = 4
}
