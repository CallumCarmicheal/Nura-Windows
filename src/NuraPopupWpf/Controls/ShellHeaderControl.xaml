<UserControl x:Class="NuraPopupWpf.Controls.ShellHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:NuraPopupWpf.Controls"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Border Width="40"
                    Height="40"
                    CornerRadius="16"
                    Background="#10FFFFFF"
                    BorderBrush="#22FFFFFF"
                    BorderThickness="1"
                    VerticalAlignment="Center">
                <TextBlock Text="n"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="White" />
            </Border>

            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding CurrentDevice.Name}"
                           Foreground="White"
                           FontSize="14"
                           FontWeight="SemiBold" />
                
                <TextBlock Text="{Binding CurrentBatteryText}"
                           Foreground="{StaticResource SubtleTextBrush}"
                           FontSize="12">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <local:ExpandedProfileSelectorControl Grid.Column="1" Margin="64,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Stretch"
                                              Profiles="{Binding CurrentProfiles}"
                                              SelectedProfile="{Binding CurrentProfile, Mode=TwoWay}"
                                              Visibility="{Binding IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                              d:Visibility="Visible" />

        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <Button Command="{Binding ToggleExpandedCommand}"
                    Style="{StaticResource IconChromeButtonStyle}"
                    Margin="0,0,8,0">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           FontSize="15"
                           Foreground="White">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="&#xE740;" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                    <Setter Property="Text" Value="&#xE73F;" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Button>
            <Button Style="{StaticResource IconChromeButtonStyle}">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           FontSize="15"
                           Foreground="White"
                           Text="&#xE13D;" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
